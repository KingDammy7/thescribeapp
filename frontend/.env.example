# Local dev: leave unset — Vite proxies /api to localhost:5000 (see vite.config.js).
# Production (Netlify): set to your deployed backend's API URL, e.g.
# VITE_API_URL=https://the-scribe-backend.onrender.com/api
VITE_API_URL=
